# Migrating to 1.0.0

impaktfull_ui renames public APIs through deprecation, not by breaking them:

1. A release adds the new name and keeps the old one as a `@Deprecated` alias. Your code keeps compiling; the analyzer shows a deprecation warning that names the replacement.
2. The old names keep working for every 0.x release.
3. 1.0.0 removes all of them at once.

This guide lists every deprecated API that 1.0.0 removes. The [CHANGELOG](../../CHANGELOG.md) lists the release each deprecation shipped in, under **Deprecations**.

## Migrate automatically

Every rename below ships with a data-driven fix (`lib/fix_data.yaml`), so `dart fix` rewrites your code for you:

```bash
dart fix --dry-run   # show what would change
dart fix --apply     # change it
```

Do this on any 0.x release before upgrading to 1.0.0. After 1.0.0 the old names no longer exist, so there is nothing left for `dart fix` to match.

## Misspelled and unprefixed names

Every public API of this package starts with `ImpaktfullUi` and is spelled correctly. The names below did not.

### Classes, enums and typedefs

| Deprecated | Replacement | Why |
| --- | --- | --- |
| `ImpaktfullUiCarrousel` | `ImpaktfullUiCarousel` | Misspelled |
| `ImpaktfullUiCarrouselTheme` | `ImpaktfullUiCarouselTheme` | Misspelled |
| `ImpaktfullUiCarrouselAssetsTheme` | `ImpaktfullUiCarouselAssetsTheme` | Misspelled |
| `ImpaktfullUiCarrouselColorTheme` | `ImpaktfullUiCarouselColorTheme` | Misspelled |
| `ImpaktfullUiCarrouselDimensTheme` | `ImpaktfullUiCarouselDimensTheme` | Misspelled |
| `ImpaktfullUiCarrouselTextStyleTheme` | `ImpaktfullUiCarouselTextStyleTheme` | Misspelled |
| `ImpaktfullUiCheckBox` | `ImpaktfullUiCheckbox` | Matches its theme, `ImpaktfullUiCheckboxTheme` |
| `ImpaktfullUiCheckBoxListItem` | `ImpaktfullUiCheckboxListItem` | Same |
| `ImpaktfullUiCheckBoxListItemType` | `ImpaktfullUiCheckboxListItemType` | Same |
| `ImpaktfullUiCheckBoxListItemTheme` | `ImpaktfullUiCheckboxListItemTheme` | Same |
| `ImpaktfullUiCheckBoxListItemAssetsTheme` | `ImpaktfullUiCheckboxListItemAssetsTheme` | Same |
| `ImpaktfullUiCheckBoxListItemColorTheme` | `ImpaktfullUiCheckboxListItemColorTheme` | Same |
| `ImpaktfullUiCheckBoxListItemDimensTheme` | `ImpaktfullUiCheckboxListItemDimensTheme` | Same |
| `ImpaktfullUiCheckBoxListItemTextStyleTheme` | `ImpaktfullUiCheckboxListItemTextStyleTheme` | Same |
| `ImpaktfullUIAdaptivePageRoute` | `ImpaktfullUiAdaptivePageRoute` | Wrong casing (`UI`) |
| `ImpaktfullUiThemeBuidler` | `ImpaktfullUiThemeBuilder` | Misspelled |
| `ImpaktfullUiAlignment` | `ImpaktfullUiDropdownAlignment` | Only used by the dropdown; the old name read like a general alignment |
| `DefaultTheme` | `ImpaktfullUiDefaultTheme` | Missing prefix |
| `TableColumnConfig` | `ImpaktfullUiTableColumnConfig` | Missing prefix |
| `CustomBadgeWidget` | `ImpaktfullUiCustomBadgeWidget` | Missing prefix |
| `CommandMenuWindow` | `ImpaktfullUiCommandMenuWindow` | Missing prefix |
| `InputFieldValidator` | `ImpaktfullUiInputFieldValidator` | Missing prefix |
| `PasswordStrengthIndicatorRequirement` | `ImpaktfullUiPasswordStrengthIndicatorRequirement` | Missing prefix |
| `CircleProgressPainter` | `ImpaktfullUiCircleProgressPainter` | Missing prefix |
| `HalfCircleProgressPainter` | `ImpaktfullUiHalfCircleProgressPainter` | Missing prefix |
| `VirtualKeyboardConfig` | `ImpaktfullUiVirtualKeyboardConfig` | Missing prefix |
| `ImpaktfullUiBBLicenseLocalizations` | `ImpaktfullUiBBLicensesLocalizations` | Matches its building block, `ImpaktfullUiBBLicenses` |

The deprecated names are type aliases of their replacement, so `is` checks, generics and theme overrides behave exactly the same with either name.

### Constructors

| Deprecated | Replacement |
| --- | --- |
| `ImpaktfullUiCheckBox.indermediate(...)` | `ImpaktfullUiCheckbox.indeterminate(...)` |
| `ImpaktfullUiCheckBoxListItem.indermediate(...)` | `ImpaktfullUiCheckboxListItem.indeterminate(...)` |

### Parameters, fields and getters

| Class | Deprecated | Replacement |
| --- | --- | --- |
| `ImpaktfullUiCheckbox` | `onChangedInterpediate` | `onChangedIndeterminate` |
| `ImpaktfullUiCheckboxListItem` | `onChangedInterpediate` | `onChangedIndeterminate` |
| `ImpaktfullUiCheckboxAssetsTheme` | `indermediate` (constructor, `copyWith`, getter) | `indeterminate` |
| `ImpaktfullUiPasswordStrengthIndicatorAssetsTheme` | `leadingRequerement` (constructor, getter) | `leadingRequirement` |
| `ImpaktfullUiPasswordStrengthIndicatorAssetsTheme` | `leadingRequerementIsMet` (constructor, getter) | `leadingRequirementIsMet` |
| `ImpaktfullUiComponentsTheme` | `carrousel` (constructor, `copyWith`, getter) | `carousel` |
| `ImpaktfullUiTouchFeedback` | `toolTip` (constructor, getter) | `tooltip` |
| `ImpaktfullUiListItemType` | `simpleLisItemType` | `simpleListItemType` |

### Enum values

| Deprecated | Replacement |
| --- | --- |
| `ImpaktfullUiWysiwygAction.orderdList` | `ImpaktfullUiWysiwygAction.orderedList` |

`orderdList` is a constant that refers to `orderedList`: a `switch` over `ImpaktfullUiWysiwygAction` must handle `orderedList`, and `ImpaktfullUiWysiwygAction.values` contains it only once.

### Parameters without a replacement

| Class | Deprecated | Migration |
| --- | --- | --- |
| `ImpaktfullUiListView.separated` | `required` | Remove the argument. It was added by mistake and never had an effect. |
| `ImpaktfullUiListView.child` | `required` | Remove the argument. It was added by mistake and never had an effect. |

`dart fix` cannot remove these arguments for you.

### Required again in 1.0.0

To accept both the old and the new name, these constructor parameters are optional during 0.x, with an `assert` that one of the two names is passed. In 1.0.0 the new name is `required` again:

- `ImpaktfullUiCheckboxAssetsTheme(indeterminate:)`
- `ImpaktfullUiComponentsTheme(carousel:)`

`ImpaktfullUiPasswordStrengthIndicatorAssetsTheme(leadingRequirement:, leadingRequirementIsMet:)` were `required` but nullable. In 1.0.0 they are `required` again.

## Consistent component theme names

Every component theme names its parts the same way: `assets` (`*AssetsTheme`), `colors` (`*ColorTheme`), `dimens` (`*DimensTheme`), `durations` (`*DurationsTheme`), `shadows` (`*ShadowsTheme`) and `textStyles` (`*TextStyleTheme`). The classes and fields below did not. `dart fix` migrates all of them.

### Classes

| Deprecated | Replacement |
| --- | --- |
| `ImpaktfullUiTableHeaderItemTextStylesTheme` | `ImpaktfullUiTableHeaderItemTextStyleTheme` |
| `ImpaktfullUiSnackyConfiguratorTextStylesTheme` | `ImpaktfullUiSnackyConfiguratorTextStyleTheme` |
| `ImpaktfullUiTableRowItemTextStylesTheme` | `ImpaktfullUiTableRowItemTextStyleTheme` |
| `ImpaktfullUiHorizontalTabTextStylesTheme` | `ImpaktfullUiHorizontalTabTextStyleTheme` |
| `ImpaktfullUiCmsHeaderTextStylesTheme` | `ImpaktfullUiCmsHeaderTextStyleTheme` |
| `ImpaktfullUiButtonTextStylesTheme` | `ImpaktfullUiButtonTextStyleTheme` |
| `ImpaktfullUiListViewTextStylesTheme` | `ImpaktfullUiListViewTextStyleTheme` |
| `ImpaktfullUiInputFieldTextStylesTheme` | `ImpaktfullUiInputFieldTextStyleTheme` |
| `ImpaktfullUiTableRowTextStylesTheme` | `ImpaktfullUiTableRowTextStyleTheme` |
| `ImpaktfullUiBadgeTextStylesTheme` | `ImpaktfullUiBadgeTextStyleTheme` |
| `ImpaktfullUiLoadingIndicatorAssetTheme` | `ImpaktfullUiLoadingIndicatorAssetsTheme` |
| `ImpaktfullUiTooltipDurationTheme` | `ImpaktfullUiTooltipDurationsTheme` |
| `ImpaktfullUiNotificationBadgeDurationTheme` | `ImpaktfullUiNotificationBadgeDurationsTheme` |
| `ImpaktfullUiSidebarNavigationItemDurationTheme` | `ImpaktfullUiSidebarNavigationItemDurationsTheme` |
| `ImpaktfullUiDropdownShadowTheme` | `ImpaktfullUiDropdownShadowsTheme` |
| `ImpaktfullUiButtonShadowTheme` | `ImpaktfullUiButtonShadowsTheme` |
| `ImpaktfullUiAutoCompleteShadowTheme` | `ImpaktfullUiAutoCompleteShadowsTheme` |

The deprecated names are type aliases of their replacement.

`ImpaktfullUiTextStylesTheme` (the `textStyles` of `ImpaktfullUiTheme`, with `onCanvas`, `onCard`, ...) keeps its name: it holds a set of text style themes, and `ImpaktfullUiTextStyleTheme` is already the name of one of them (`display` and `text`).

### Parameters, fields and getters

| Class | Deprecated | Replacement |
| --- | --- | --- |
| `ImpaktfullUiButtonTheme` | `shadow` (constructor, `copyWith`, getter) | `shadows` |
| `ImpaktfullUiComponentsTheme` | `checkBoxListItem` (constructor, `copyWith`, getter) | `checkboxListItem` |

`ImpaktfullUiComponentsTheme(checkboxListItem:)` is optional during 0.x, with an `assert` that `checkboxListItem` or `checkBoxListItem` is passed. In 1.0.0 it is `required` again.

## Global theme and locale

The top-level `theme` and `locale` getters were global variables, set by `ImpaktfullUiApp`. A global can not follow a theme override lower in the tree, and a widget that reads it does not rebuild when the theme changes. They are deprecated: read the theme and the locale from the widget tree instead.

| Deprecated | Replacement |
| --- | --- |
| `theme` | `ImpaktfullUiTheme.of(context)` |
| `locale` | `Localizations.localeOf(context)` |
| `setImpaktfullUiTheme(theme)` | `ImpaktfullUiApp(impaktfullUiTheme: theme)` |
| `setImpaktfullUiLocale(locale)` | `ImpaktfullUiApp(locale: locale)` |
| `setImpaktfullUiTestTheme(theme)` | Pump the widget under test in an `ImpaktfullUiApp(impaktfullUiTheme: theme)` |

`dart fix` can not migrate these: the replacement needs a `BuildContext`. Replace them by hand:

```dart
// Before
@override
Widget build(BuildContext context) {
  return Text('Hello', style: theme.textStyles.onCanvas.text.small);
}

// After
@override
Widget build(BuildContext context) {
  final theme = ImpaktfullUiTheme.of(context);
  return Text('Hello', style: theme.textStyles.onCanvas.text.small);
}
```

To find every use, remove `theme` from the import and let the analyzer list the places that no longer compile:

```dart
import 'package:impaktfull_ui/impaktfull_ui.dart' hide theme, locale;
```

In code without a `BuildContext` (a service, a static helper), pass the theme (or the value you need from it) as a parameter, or use `ImpaktfullUiTheme.getDefault()` if you never change the theme.

Until 1.0.0 `ImpaktfullUiApp` keeps setting the global theme and locale, so the deprecated getters keep returning what they returned before. The components and building blocks of this package no longer read them.

## Extensions no longer exported

The package exported extensions without the `ImpaktfullUi` prefix on types of Flutter and Dart. They added members such as `format`, `isSameDay`, `tomorrow`, `bold` and `start` to every `DateTime`, `TextStyle` and `EdgeInsetsGeometry` of your app, where they clash with extensions of your own or of other packages. 1.0.0 no longer exports them.

| Deprecated | Migration |
| --- | --- |
| `ResponsiveExtension` on `BuildContext` (`context.isSmallScreenOrBigger`, ...) | The static helpers of `ImpaktfullUiResponsiveLayout`: `context.isSmallScreenOrBigger` becomes `ImpaktfullUiResponsiveLayout.isSmallOrBigger(context)`, `context.isMediumScreenOrSmaller` becomes `ImpaktfullUiResponsiveLayout.isMediumOrSmaller(context)`, and so on |
| `TextStyleExtension` (`light`, `medium`, `semiBold`, `bold`, `withOpacity`) | `style.copyWith(fontWeight: FontWeight.w300)` (`w500`, `w600`, `w700`), `style.copyWith(color: style.color?.withValues(alpha: opacity))` |
| `DateTimeExtensions` (`format`, `isSameDay`, `tomorrow`, `startOfTheDay`, ...) | Copy it into your app |
| `getDayForMonthWithFallback` | Copy it into your app |
| `BorderRadiusGeometryExtension` (`topStart`, ..., `value`, `copyWith`) | Copy it into your app, or use `BorderRadiusGeometry.resolve(textDirection)` |
| `EdgeInsetsGeometryExtension` (`start`, `end`, `top`, `bottom`) | Copy it into your app, or use `EdgeInsetsGeometry.resolve(textDirection)` |
| `ThemeDataExtension` (`removeUnwantedBehavior`) | Copy it into your app. `ImpaktfullUiApp` keeps applying it to the `ThemeData` you pass |

The extensions and each of their members are deprecated, so the analyzer points at every use (an extension member is only reported when the member itself is deprecated). They keep working until 1.0.0.

`dart fix` can not migrate these: there is no replacement with the same shape. To copy an extension into your app, take its source from `lib/src/deprecated/deprecated_extensions.dart` of the version you use (the implementations are in `lib/src/util/extension/`). Until 1.0.0 the deprecated extension is still exported, and two extensions with the same members on the same type make every use ambiguous. Either give your copy's members other names, or hide the deprecated one in the files that use your copy:

```dart
import 'package:impaktfull_ui/impaktfull_ui.dart' hide DateTimeExtensions;
import 'package:my_app/util/date_time_extensions.dart';
```

After upgrading to 1.0.0, remove the `hide`.

## Right-to-left layouts

Parameters that only accepted a physical `Alignment` are replaced by an `AlignmentGeometry` parameter, so you can pass an `AlignmentDirectional` that follows the reading direction.

| Class | Deprecated | Replacement |
| --- | --- | --- |
| `ImpaktfullUiScreen` | `fabAlignment` (constructor, getter) | `floatingActionButtonAlignment` |
| `ImpaktfullUiAdaptiveScreen` | `fabAlignment` (constructor, getter) | `floatingActionButtonAlignment` |

`dart fix` renames the constructor argument and keeps its value, so a physical `Alignment` you passed stays where it was. Without an argument the fab is now placed at `AlignmentDirectional.bottomEnd`: the bottom right corner in a left-to-right layout (as before) and the bottom left corner in a right-to-left layout. The deprecated `fabAlignment` getter returns the left-to-right value of `floatingActionButtonAlignment`; replace reads of it by hand, since the new getter is an `AlignmentGeometry`.

## `dart:io` File APIs

A `File` of `dart:io` can not be read on the web, so the APIs that take one are deprecated. Their replacements take bytes (`Uint8List`) or an `XFile` of [`cross_file`](https://pub.dev/packages/cross_file) (which `image_picker` and `file_selector` return) and work on every platform, including the web with JS and Wasm.

| Deprecated | Replacement |
| --- | --- |
| `ImpaktfullUiGalleryItem.file(file: file)` | `ImpaktfullUiGalleryItem.xFile(xFile: XFile(file.path))` or `ImpaktfullUiGalleryItem.bytes(bytes: bytes)` |
| `ImpaktfullUiGalleryItem.file` (field) | `ImpaktfullUiGalleryItem.xFile` or `ImpaktfullUiGalleryItem.bytes` |
| `ImpaktfullUiImageCropController.cropImageFile(cropInfo:, file: file)` | `cropXFile(cropInfo:, xFile: XFile(file.path))` or `cropImageBytes(cropInfo:, bytes: bytes)` |
| `ImpaktfullUiImageCropCropper.cropImage(imageFile: file)` | `cropImage(xFile: XFile(file.path))` or `cropImage(imageBytes: bytes)` |

`dart fix` can not migrate these for you: the replacement takes a different type. Add `cross_file` to your `pubspec.yaml` to create an `XFile` yourself.

Until 1.0.0 the deprecated APIs keep working on Android, iOS, macOS, Windows and Linux. `cropImageFile` now crops the file instead of throwing `imageFile is not supported yet`. On the web they still compile (the parameter accepts any object there), but reading the file throws an `UnsupportedError`.

## Internal changes

These are not part of the public API and need no migration, as long as you import `package:impaktfull_ui/impaktfull_ui.dart` and never a file under `lib/src/` (which is unsupported and not covered by these deprecations).

**Files renamed under `lib/src/`:**

| Before | After |
| --- | --- |
| `components/carrousel/carrousel.dart` | `components/carousel/carousel.dart` |
| `components/carrousel/carrousel_style.dart` | `components/carousel/carousel_style.dart` |
| `components/check_box_list_item/check_box_list_item.dart` | `components/checkbox_list_item/checkbox_list_item.dart` |
| `components/check_box_list_item/check_box_list_item_style.dart` | `components/checkbox_list_item/checkbox_list_item_style.dart` |
| `components/calendar/widget/calender_types/` | `components/calendar/widget/calendar_types/` |
| `components/progress_indicator/widget/cirlce_progress_indicator.dart` | `components/progress_indicator/widget/circle_progress_indicator.dart` |
| `components/progress_indicator/widget/half_cirlce_progress_indicator.dart` | `components/progress_indicator/widget/half_circle_progress_indicator.dart` |
| `components/segmented_control/widget/segment_control_item.dart` | `components/segmented_control/widget/segmented_control_item.dart` |
| `components/wysiwyg/widget/actions/wysiwig_actions.dart` | `components/wysiwyg/widget/actions/wysiwyg_actions.dart` |

**Classes renamed that were never exported:**

| Before | After |
| --- | --- |
| `CheckboxType` | `ImpaktfullUiCheckboxType` (now exported: the type of `ImpaktfullUiCheckbox.type`) |
| `CommanderController` | `ImpaktfullUiCommandMenuController` (now exported: passed to the `ImpaktfullUiCommandMenu.builder` callback) |
| `CommanderListener` | `ImpaktfullUiCommandMenuListener` (now exported: used by `ImpaktfullUiCommandMenuController.attach`) |
| `ImpaktfulluiCalendarListItem` | `ImpaktfullUiCalendarListItemModel` (`ImpaktfullUiCalendarListItem` is already the widget that renders it) |
| `ImpaktfullSnackyBuilder` | `ImpaktfullUiSnackyBuilder` |
| `ImpaktfullSnackyTextType` | `ImpaktfullUiSnackyTextType` |
| `ImpaktfullUiSegmentControlItem` | `ImpaktfullUiSegmentedControlItem` |
| `ImpaktfullUiNavBarSmallLoadingWrapper.toolTip` | `ImpaktfullUiNavBarSmallLoadingWrapper.tooltip` |

**Example app:** the carousel showcase moved from `items/carrousel/` to `items/carousel/`.

**Extensions under `lib/src/util/extension/`:** they are the library's own, not exported, copies. `DateTimeExtensions`, `TextStyleExtension`, `BorderRadiusGeometryExtension` and `EdgeInsetsGeometryExtension` in those files are renamed to `InternalDateTimeExtension`, `InternalTextStyleExtension`, `InternalBorderRadiusGeometryExtension` and `InternalEdgeInsetsGeometryExtension`, and `getDayForMonthWithFallback` to `dayForMonthWithFallback`. The deprecated, exported ones moved to `lib/src/deprecated/deprecated_extensions.dart`.

**Global theme:** the global theme and locale are stored in `lib/src/theme/global_theme.dart`. `lib/src/theme/theme_configurator.dart` only holds the deprecated getters and setters.

**Behavior changes:**

- The building blocks (`ImpaktfullUiBBForgetPassword`, `ImpaktfullUiBBLicenses`, `ImpaktfullUiBBLogin`, `ImpaktfullUiBBNotFound`, `ImpaktfullUiBBRegister`, `ImpaktfullUiBBVerifyRegisterCode`) and the preview of `ImpaktfullUiImageCrop` read their theme with `ImpaktfullUiTheme.of(context)` instead of the global `theme`. They now follow a theme override around them and rebuild when the theme changes. Before, they used the theme of the `ImpaktfullUiApp` (or of the last theme override that was built).
- `ImpaktfullUiFocusFeedback` without an `ImpaktfullUiApp` above it draws its focus ring with `ImpaktfullUiTheme.getDefault()` instead of the global theme.
- The test theme set with the deprecated `setImpaktfullUiTestTheme` is only returned by the deprecated `theme` getter: components always use the theme of the widget tree.

## Planned breaking changes

These are not in place yet. They change behavior, so they wait for 1.0.0.

### `ImpaktfullUiResponsiveLayout` size classes

`ImpaktfullUiResponsiveLayout.build()` and its static helpers (and the `context.is*Screen*` extensions) use the breakpoints differently:

| Size | `build()` builder | Helpers |
|------|-------------------|---------|
| small | `width <= breakpointMedium` (`breakpointSmall` is not used) | `isSmallOrSmaller`: `width <= breakpointSmall` |
| medium | `width <= breakpointLarge` | `isMediumOrSmaller`: `width <= breakpointMedium` |
| large | `width <= breakpointExtraLarge` | `isLargeOrSmaller`: `width <= breakpointLarge` |
| extraLarge | `width > breakpointExtraLarge` | `isExtraLargeOrBigger`: `width >= breakpointLarge` |

So at 800px (default theme) `build()` uses the `small` builder while `isSmallOrSmaller` is false. `isLargeOrSmaller` and `isExtraLargeOrBigger` are also both true at exactly `breakpointLarge`. 1.0.0 will make `build()` and the helpers use the same size classes. Check the layouts that depend on either of them when you upgrade.
